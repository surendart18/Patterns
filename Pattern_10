#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    int val=1;
    for(int i=1;i<=n;i++)
    {
        for(int j=n;j>=i;j--)
        {
            cout<<val<<" ";
            val++;
        }
        cout<<endl;
        val=1;
    }
    return 0;
}
